package com.flipcart.mobiles;

public interface Parent {
	//we Don't have any type of constructor,methods are not defined only methods are called  
	//interface may have or not abstract keyword 
	
	abstract void data();
}
